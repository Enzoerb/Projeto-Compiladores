programa

  inteiro a.
  decimal b, c, d, e, f.
  texto t1.
  a := 2.
  e := 1.0.
  d := 1.0.
  c := 1.0.
  b := c/d.

  se ( e < b ) {
     escreva (b).
  }
  senao {
     escreva (e).
  }
  d := 1.0.
  enquanto (d > b) {
    leia(b).
  }

  faca {
    leia (b).
  } enquanto(e > b).

fimprog.